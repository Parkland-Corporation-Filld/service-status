{
   "name":"filld/filld-service-status",
   "description": "A simple status response to the root `/` route. It returns the App name, version, environment, and git sha.",
   "autoload":{
      "psr-4":{
         "Filld\\ServiceStatus\\":"src/"
      }
    },
    "require": {
        "illuminate/config": "~5.0"
    }
}